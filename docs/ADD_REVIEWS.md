# 리뷰 데이터 추가 가이드

이 가이드는 CSV 파일을 사용하여 리뷰 데이터를 클라우드 데이터베이스에 추가하는 방법을 설명합니다.

## 📋 필요한 정보

### 필수 항목
- **username**: 리뷰 작성자의 사용자명 (DB에 이미 존재해야 함)
- **restaurant_name**: 음식점 이름 (DB에 이미 존재해야 함)
- **stars**: 별점 (1.0 ~ 5.0)
- **text**: 리뷰 내용

### 선택 항목
- **date**: 리뷰 작성 날짜 (형식: YYYY-MM-DD 또는 YYYY/MM/DD)

### 자동 생성/계산되는 항목
다음 항목은 자동으로 처리됩니다:
- **created_at**: DB 삽입 시간 (자동)
- **useful**: 초기값 0
- **absa_features**: 빈 객체 (나중에 ABSA 모델로 계산)
- **is_taste_test**: False (일반 리뷰)

---

## 📝 CSV 파일 준비

### CSV 파일 형식

`data/reviews_to_add.csv` 파일을 다음과 같이 작성합니다:

```csv
username,restaurant_name,stars,text,date
testuser,우만동족발집 아주대점,5.0,족발이 정말 맛있어요! 고기가 부드럽고 야채도 신선해요.,2025-01-15
alice,우만동족발집 아주대점,4.5,가성비 좋고 맛있습니다. 포장도 가능해요.,2025-01-16
bob,우만동족발집 아주대점,4.0,맛있지만 양이 조금 적은 것 같아요.,
```

### 주의사항

1. **첫 줄은 헤더**: `username,restaurant_name,stars,text,date`
2. **username과 restaurant_name**: 반드시 DB에 이미 존재해야 함
3. **stars**: 1.0 ~ 5.0 사이의 숫자 (소수점 가능)
4. **text**: 리뷰 내용 (쉼표가 포함된 경우 큰따옴표로 감싸기)
5. **date**: 선택 항목, 비어있으면 현재 시간 사용
6. **UTF-8 인코딩**: CSV 파일은 반드시 UTF-8로 저장

### CSV에서 특수문자 처리

리뷰 내용에 쉼표나 줄바꿈이 있는 경우:

```csv
username,restaurant_name,stars,text,date
testuser,우만동족발집 아주대점,5.0,"정말 맛있어요! 족발, 보쌈 둘 다 추천합니다.",2025-01-15
alice,우만동족발집 아주대점,4.5,"가격도 착하고
맛도 좋아요!",2025-01-16
```

---

## 🚀 스크립트 실행

### 기본 실행 (클라우드 DB)

```bash
# 환경변수 설정 후 실행
export RAILWAY_DATABASE_URL="your_database_url"
python scripts/add_reviews_to_cloud.py data/reviews_to_add.csv
```

### 실행 결과 예시

성공적으로 실행되면 다음과 같은 메시지가 표시됩니다:

```
============================================================
🚀 클라우드 Railway DB에 리뷰 추가
============================================================
📁 CSV 파일: data/reviews_to_add.csv
🗄️  DB: interchange.proxy.rlwy.net:52092
============================================================

📖 CSV 파일 읽는 중: data/reviews_to_add.csv
[1] ✅ 파싱 성공: testuser -> 우만동족발집 아주대점 (5.0점)
[2] ✅ 파싱 성공: alice -> 우만동족발집 아주대점 (4.5점)
✅ 2개 리뷰 발견

🔄 DB에 추가 중...
✅ 추가: testuser -> 우만동족발집 아주대점 (5.0점)
✅ 추가: alice -> 우만동족발집 아주대점 (4.5점)

🎉 완료: 2개 추가, 0개 스킵

🔄 음식점 통계 업데이트 중...
  📊 우만동족발집 아주대점: 0.0점(0개) -> 4.75점(2개)
✅ 통계 업데이트 완료

✅ 모든 작업 완료!
```

---

## 📊 자동 업데이트 항목

리뷰가 추가되면 다음 항목이 자동으로 업데이트됩니다:

1. **음식점 평균 별점** (`stars`): 모든 리뷰의 평균
2. **음식점 리뷰 수** (`review_count`): 총 리뷰 개수

---

## 🔧 트러블슈팅

### 문제 1: "사용자를 찾을 수 없습니다: testuser"

**원인**: CSV의 username이 DB에 없습니다.

**해결**:
1. 프론트엔드에서 회원가입하여 사용자 생성
2. 또는 DB에 이미 존재하는 username 사용

### 문제 2: "음식점을 찾을 수 없습니다: 우만동족발집"

**원인**: CSV의 restaurant_name이 DB에 없습니다.

**해결**:
1. `add_restaurant_to_cloud.py` 스크립트로 음식점 먼저 추가
2. 또는 DB에 이미 존재하는 음식점 이름 사용 (정확히 일치해야 함)

### 문제 3: "stars는 1.0~5.0 사이여야 합니다"

**원인**: 별점이 유효 범위를 벗어났습니다.

**해결**:
- CSV의 stars 값을 1.0 ~ 5.0 사이로 수정

### 문제 4: "text 누락. 건너뜀"

**원인**: 리뷰 내용이 비어있습니다.

**해결**:
- CSV의 text 컬럼에 리뷰 내용 작성

### 문제 5: "이미 존재하는 리뷰"

**원인**: 같은 사용자가 같은 음식점에 같은 내용의 리뷰를 이미 작성했습니다.

**해결**:
- 정상 동작입니다. 중복 방지 기능이 작동한 것입니다.
- 리뷰 내용을 조금 다르게 수정하거나 다른 음식점/사용자 조합 사용

---

## 💡 팁

### 1. Excel에서 CSV 작성하기

1. Excel에서 데이터 입력
2. "다른 이름으로 저장" -> **"CSV UTF-8 (쉼표로 분리)"** 선택
3. 저장 후 스크립트 실행

### 2. 대량 리뷰 추가

여러 사용자의 리뷰를 한 번에 추가할 수 있습니다:

```csv
username,restaurant_name,stars,text,date
user1,우만동족발집 아주대점,5.0,최고예요!,2025-01-15
user2,우만동족발집 아주대점,4.5,맛있어요,2025-01-16
user3,우만동족발집 아주대점,4.0,괜찮아요,2025-01-17
user1,다른음식점,3.5,보통이에요,2025-01-18
```

### 3. 날짜 형식

다음 형식들을 모두 지원합니다:
- `2025-01-15`
- `2025/01/15`
- `2025.01.15`
- `2025-01-15 14:30:00`

### 4. 여러 CSV 파일 순차 추가

```bash
python scripts/add_reviews_to_cloud.py data/reviews_batch1.csv
python scripts/add_reviews_to_cloud.py data/reviews_batch2.csv
python scripts/add_reviews_to_cloud.py data/reviews_batch3.csv
```

---

## 📚 참고 자료

- [음식점 추가 가이드](ADD_RESTAURANTS.md)
- [프로젝트 README](../README.md)

---

**작성일**: 2025-11-16  
**최종 수정**: 2025-11-16

